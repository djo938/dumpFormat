MISC
    -creer l'adresse mail dumpformat@djoproject.net

RIGHT
    -XXX gerer les droits d'acces au donnees XXX

XML FORMAT
    -adapt the DTD
    -add the DTD in the xml file
        
API
    faire une classe permettant de charger/decharger un dump
        identifier un validateur (qui verifie que la dtd s'applique bien au xml chargé)
        identifier un parser xml en python (sax ou dom)
    mettre des add/set pour les champs
        faire une abstraction intelligente, que ce ne soit pas bete et mechant
        
SOFT
    faire une interface graphique
        -voir s'il n'y en a pas deja des existantes (genre pour mobib extractor)

######
    pour l'interpretation des donnee
    
InformationStored XML
    convertToString
        name
        path
        sectorid
        byteStart
        length
        
    convertToInt
        name
        path
        sectorid
        byte/bit
        start
        length
        
    conditionnel structure
        if value[name] ==
            #make other convert
